worker_processes auto;
error_log /var/log/nginx/error.log warn;
events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" $status $body_bytes_sent';
    access_log /var/log/nginx/access.log main;

    sendfile on;
    keepalive_timeout 65;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    client_max_body_size 50M;

    server {
        listen 80 default_server;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;

        # /api/* → ffred backend
        location /api/ {
            proxy_pass https://ffred-ddd989.amvera.io;
            proxy_set_header Host ffred-ddd989.amvera.io;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_protocols TLSv1.2 TLSv1.3;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }

        # WebSocket /ws/* → ffred
        location /ws/ {
            proxy_pass https://ffred-ddd989.amvera.io;
            proxy_set_header Host ffred-ddd989.amvera.io;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_ssl_server_name on;
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # /health прямой роут (бэк)
        location = /health {
            proxy_pass https://ffred-ddd989.amvera.io/health;
            proxy_set_header Host ffred-ddd989.amvera.io;
            proxy_ssl_server_name on;
        }

        # Статика
        location / {
            try_files $uri $uri/ $uri.html =404;
        }

        # Custom 404 page (если есть в репо)
        error_page 404 /404.html;

        # cache headers для статики (картинки, шрифты, CSS, JS)
        location ~* \.(jpg|jpeg|png|webp|gif|ico|svg|css|js|woff2?|ttf|eot)$ {
            expires 30d;
            add_header Cache-Control "public, immutable";
        }
    }
}
